package ${packageName}.models;

import ${packageName}.models.base.BaseEntity;
import lombok.Data;
${importPath}

//表名称 ${tableName}

@Data
public class ${className} extends BaseEntity {

	private static final long serialVersionUID = 1L;

	public ${className}() {
		super();
	}
	
${function}
}

